# 共享内核第二阶段-自动分块

## L1 摘要
- 模块：题目录入
- 日期：2026-04-20
- 状态：已完成
- 结论：shared_core 已能直接从 `DocNode[]` 自动识别题块，文科/理科默认优先走共享内核自动分块，英语仍保留旧吞并逻辑兜底。

## L2 现象与根因
- 现象：共享内核第一阶段只能包装外部已经识别好的 `q_nodes`，真正的题目起点和边界仍依赖 `core_parser.py` 旧逻辑，复用价值有限。
- 根因：`shared_core/question_core.py` 缺少“从节点流自动识别 raw starts 并吞并成题块”的主入口；文科材料正则也比旧逻辑更窄。

## L3 改动与验证
- 改动文件：
  - `shared_core/question_core.py`
  - `shared_core/strategies.py`
  - `shared_core/__init__.py`
  - `墨痕快刀/core_parser.py`
  - `test_shared_core_flow.py`
- 测试样例：
  - 地理材料题：`完成1～2题`
  - 化学图文题：题干 + 图片段 + 选项 + 下一题
  - 原有共享内核回归样例
- 测试命令：
  - `python -m py_compile "shared_core/strategies.py" "shared_core/question_core.py" "shared_core/__init__.py" "墨痕快刀/core_parser.py" "test_shared_core_flow.py"`
  - `python test_shared_core_flow.py`
- 结果：
  - `py_compile` 通过
  - `test_shared_core_flow.py` 输出 `ALL PASSED`

## L4 深层备注
- 风险：英语模块仍主要依赖旧的 VIP / Passage 吞并逻辑，这次没有强行切到 shared_core，避免一次改太多。
- 下次排查入口：
  - `shared_core/question_core.py` 的 `_detect_raw_starts()` 和 `_merge_raw_starts()`
  - `shared_core/strategies.py` 的 `READING_PATTERNS`
  - `墨痕快刀/core_parser.py` 的 `question_units` 构建逻辑
- 关联问题：
  - `2026-04-11-共享内核第一阶段.md`
  - `2026-04-20-工作区整理策略.md`
