# 数学文件名补充到理科自动分科

## L1 摘要
- 模块：题目录入
- 日期：2026-04-22
- 状态：已完成
- 结论：`core_parser.detect_subject()` 已把“数学”补进理科文件名关键词，后续带“数学”字样的题目文档会直接落到理科配置，不再依赖最终兜底分支。

## L2 现象与根因
- 现象：项目主入口的自动分科里，理科文件名关键词包含 `物理/化学/生物/科学/理综`，但没有显式包含“数学”。
- 根因：共享策略层已经把“数学”视为理科，但 `墨痕快刀/core_parser.py` 的主入口自动分科条件未同步，导致数学文档只能靠内容判断或默认兜底落入理科。

## L3 改动与验证
- 改动文件：
  - `墨痕快刀/core_parser.py`
  - `test_math_subject_detection.py`
- 测试样例：
  - 模拟文档名：`八年级数学第6次限时训练.docx`
- 测试命令：
  - `python -m unittest test_math_subject_detection.py`
- 结果：
  - 1 个离线用例通过。
  - 带“数学”字样的文件名现在直接命中 `理科`，且不会误设置 `CURRENT_SUBJECT_OVERLAY`。

## L4 深层备注
- 风险：
  - 这次只补了主入口文件名关键词，没有新增数学 overlay；如果后续数学卷出现学校专属前缀题号或特殊材料题范围规则，应优先新建数学覆盖层，而不是继续往全局理科里堆特判。
- 下次排查入口：
  - 先看 `test_math_subject_detection.py`
  - 再看 `墨痕快刀/core_parser.py` 的 `detect_subject()`
- 关联问题：
  - `../格式转换/2026-04-22-安乡金海初二数学答案模板接入.md`
