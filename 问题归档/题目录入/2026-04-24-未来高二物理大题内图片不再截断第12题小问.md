# 未来高二物理大题内图片不再截断第12题小问

## L1 摘要
- 模块：题目录入
- 日期：2026-04-24
- 状态：已完成
- 结论：`课时跟踪检测(八) 摩擦力` 的第 12 题现在不会再被题内插图提前截断；`F1` 录题时会一并带入题干、图片和后续 `(1)(2)(3)` 小问。

## L2 现象与根因
- 现象：
  - `现实训练8 / 摩擦力` 第 12 题录入时，只进了大题题干。
  - 题内图片和后面的 `(1)(2)(3)` 小问没有进入插件题卡。
  - 运行日志里出现：
    - `✂️ [提前截断] 行54 [STD] 命中 obstacle: Img_DecorativeHeader_130x37`
- 根因：
  - 共享内核已经正确识别出第 12 题完整范围：
    - `question_type = subjective`
    - `source_span = (53, 60)`
    - `media_blocks = [54]`
    - `subquestions = 3 个`
  - 但 `F1` 最终执行前，会在题块内部再次跑 obstacle 截断。
  - 第 54 段的题内插图被误判成 `DecorativeHeader`，导致执行层把选区截回第 53 段，只剩题干。

## L3 改动与验证
- 改动文件：
  - `墨痕快刀/core_parser.py`
  - `test_review_prompt_mode.py`
- 规则变更：
  - `should_ignore_inline_obstacle()` 新增规则：
    - 当题型为 `subjective/material`
    - 且当前题块已被标记为 `image_related_question`
    - 且命中的 obstacle 是 `DecorativeHeader`
    - 且题块内部本身带 `media_blocks` 或 `subquestions`
    - 则把该图片视为题内插图，不再提前截断
- 测试命令：
  - `.\.venv\Scripts\python.exe -m py_compile .\墨痕快刀\core_parser.py .\test_review_prompt_mode.py`
  - `.\.venv\Scripts\python.exe .\test_review_prompt_mode.py`
  - 真实样本离线核对：
    - `.\.venv\Scripts\python.exe -` 读取 `D:\墨痕教育题目\未来-高二-物理\课时跟踪检测(八)　摩擦力.docx`
    - 确认第 12 题 `should_ignore_inline_obstacle(..., "Img_DecorativeHeader_130x37") == True`
- WPS 实机复测：
  - 样本：`D:\墨痕教育题目\未来-高二-物理\课时跟踪检测(八)　摩擦力.docx`
  - 复测后日志从原来的：
    - `✂️ [提前截断] 行54 [STD] 命中 obstacle: Img_DecorativeHeader_130x37`
  - 变为：
    - `🖼️ [忽略截断] 行54 [STD] 图片/空白段不截断`
  - 插件题卡中已能看到第 12 题的题干、题内图片和 `(1)(2)(3)` 小问。

## L4 下次排查入口
- 如果以后再出现“带图大题只录题干”的问题，先看三件事：
  - `shared_core` 是否已经识别出完整 `source_span`
  - 题块是否带 `image_related_question`
  - `墨痕快刀/core_parser.py` 的 `should_ignore_inline_obstacle()` 是否又被新的 obstacle 规则覆盖
- 这类问题优先视为“执行层二次截断”，不要先怀疑题号识别。
